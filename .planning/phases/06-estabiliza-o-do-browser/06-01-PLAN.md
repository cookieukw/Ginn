---
phase: 6
plan: 1
wave: 1
depends_on: [05-01]
requirements_addressed: [UI-01, UI-02, GAME-01]
files_modified: [DataManager.js, main.js, test-parser.js, test-engine.js]
autonomous: true
---

# Plan 06-01: Estabilização do Browser e Dual-Env ESM

**Objective**: Corrigir os erros de inicialização no browser ("fs" module not found) e garantir que os assets e dados (animals.json, jinn.png) sejam carregados corretamente via Vite.

<must_haves>
- [ ] Remoção de importações estáticas de Node (fs, path) do código que roda no browser.
- [ ] Uso de `/* @vite-ignore */` em importações dinâmicas para evitar erros de resolução do Vite.
- [ ] Correção das referências de caminho no `main.js` e `DataManager.js`.
- [ ] Verificação de que os testes locais (Node) continuam funcionando com o novo carregamento assíncrono.
</must_haves>

## Tasks

### Wave 0: Dual-Env DataManager

- [ ] **06-01-01**: Refatorar `DataManager.js` para ignorar o Vite.
    <read_first>
    - [DataManager.js](file:///home/cookie/Documents/Projetos/Ginn/DataManager.js)
    </read_first>
    <action>
    Substituir o `import('fs')` atual por um `import(/* @vite-ignore */ 'fs')` dentro do método `load()`. Adicionar uma verificação de ambiente (`typeof window === 'undefined'`) para segurança extra.
    </action>
    <acceptance_criteria>
    - Vite para de dar erro de "Failed to resolve module specifier 'fs'".
    </acceptance_criteria>

### Wave 1: Asset Path Verification

- [ ] **06-01-02**: Verificar e corrigir caminhos no `main.js`.
    <read_first>
    - [main.js](file:///home/cookie/Documents/Projetos/Ginn/main.js)
    </read_first>
    <action>
    Garantir que os caminhos para `/animals.json` e `/jinn.png` estão corretos em relação à raiz do servidor do Vite. Remover qualquer importação residual ou referência a arquivos locais.
    </action>
    <acceptance_criteria>
    - O console não mostra mais 404 para animals.json.
    </acceptance_criteria>

### Wave 2: Test Sync

- [ ] **06-01-03**: Sincronizar scripts de teste.
    <read_first>
    - [test-parser.js](file:///home/cookie/Documents/Projetos/Ginn/test-parser.js)
    </read_first>
    <action>
    Garantir que os testes locais `test-parser.js` e `test-engine.js` usem o `await manager.load()` corretamente agora que ele é assíncrono.
    </action>
    <acceptance_criteria>
    - `node test-parser.js` e `node test-engine.js` passam localmente.
    </acceptance_criteria>

## Verification

### Automated Checks
- `node test-parser.js`
- `node test-engine.js`

### Manual Verification
- Abrir o navegador com `npm run dev` e confirmar que o console está limpo e a tela de início aparece.
