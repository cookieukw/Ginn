---
phase: 1
plan: 1
wave: 1
depends_on: []
requirements_addressed: [CORE-01]
files_modified: [DataManager.js, DataStore.js, test-parser.js]
autonomous: true
---

# Plan 01-01: Fundação e Dados

**Objective**: Implementar a infraestrutura de dados para carregar, filtrar e indexar animais.

<must_haves>
- [ ] Parser para `animals.json` que extraia os 22 atributos de cada animal.
- [ ] DataStore que gerencie a lista de "Candidatos Ativos" em memória.
- [ ] Indexação de atributos em formato binário (Sim/Não) para facilitar o motor de entropia.
- [ ] Script de teste para validar o carregamento dos dados.
</must_haves>

## Tasks

### Wave 0: Setup de Validação

- [ ] **01-01-01**: Criar `test-parser.js` asserções básicas.
    <read_first>
    - [animals.json](file:///home/cookie/Documents/Projetos/Ginn/oldData/animals.json)
    </read_first>
    <action>
    Criar um script de teste que carregue o JSON e verifique se o número de animais e atributos bate com o esperado (100+ animais, 22 características por animal).
    </action>
    <acceptance_criteria>
    - `test-parser.js` existe e pode ser executado com `node test-parser.js`.
    </acceptance_criteria>

### Wave 1: Core Data Implementation

- [ ] **01-01-02**: Implementar `DataManager.js`.
    <read_first>
    - [animals.json](file:///home/cookie/Documents/Projetos/Ginn/oldData/animals.json)
    </read_first>
    <action>
    Criar a classe `DataManager` com métodos para carregar o JSON e normalizar os atributos. Deve converter valores categóricos em chaves binárias (ex: "Tamanho: Grande" -> "Tamanho_Grande").
    </action>
    <acceptance_criteria>
    - `DataManager.js` exporta uma classe funcional.
    - O método `load()` retorna uma lista de objetos animais com atributos normalizados.
    </acceptance_criteria>

- [ ] **01-01-03**: Implementar `DataStore.js`.
    <read_first>
    - [DataManager.js](file:///home/cookie/Documents/Projetos/Ginn/DataManager.js)
    </read_first>
    <action>
    Criar a classe `DataStore` para gerenciar o estado da sessão de jogo. Deve conter a lista de `activeCandidates` e métodos para filtrar animais baseados em respostas.
    </action>
    <acceptance_criteria>
    - `DataStore.js` contém a lógica de filtragem inicial.
    - Possui um método `getCandidates()` que retorna a lista atualizada.
    </acceptance_criteria>

## Verification

### Automated Tests
- `node test-parser.js`

### Manual Verification
- Inspecionar a saída de log do parser para garantir que os nomes dos atributos em Português estão preservados e as chaves binárias foram geradas corretamente.
